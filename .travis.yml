language: python
python:
  - "3.7"
env:
  - DJANGO_SETTINGS_MODULE=places_remember.config.settings
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest
